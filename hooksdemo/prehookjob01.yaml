apiVersion: batch/v1
kind: Job
metadata:
  name: prehookjob-demo01-
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  template:
    spec:
      containers:
      - name: demo
        image: alpine
        args:
        - /bin/sh
        - -c
        - echo "Command executed from Prehook Job 01"
      restartPolicy: OnFailure