apiVersion: batch/v1
kind: Job
metadata:
  name: posthookjob-demo01-
  annotations:
    argocd.argoproj.io/hook: PostSync
spec:
  template:
    spec:
      containers:
      - name: demo
        image: alpine
        args:
        - /bin/sh
        - -c
        - echo "Command executed from Posthook Job 01"
      restartPolicy: OnFailure